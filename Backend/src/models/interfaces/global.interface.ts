export interface IAddress {
  street: string; // Street address (e.g., "123 Main St")
  city: string; // City (e.g., "New York")
  state: string; // State (e.g., "NY")
  country: string; // Country (e.g., "USA")
  zipCode: string; // Postal/ZIP code (e.g., "10001")
}
